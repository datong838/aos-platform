# AIP-P1 共享壳、导航与业务可理解性交付证据

- 日期：2026-08-30
- 分支：`m1`
- 正向租户：`org-org/dev-project`
- 范围：AIP-P1-007～AIP-P1-014
- 裁决：`AIP_P1_SHARED_SHELL_BUSINESS_CONTRACT_GREEN`

## 浏览器验收

1. Codex 内置浏览器逐页进入 25 个 AIP 路由，确认 25/25 菜单入口可发现、当前页唯一高亮、深链所属分组自动展开、侧栏滚动与整体折叠不丢入口。
2. 25 页稳定态扫描结果：禁止的内部终态词 0；无精确原因的可见禁用控件 0；文档级横向溢出 0；错误的当前页标记 0。
3. 1280×720、1440×900、1920×1080 三视口回归均无文档级横向溢出；根文本放大到 200% 后仍无文档级横向溢出，交互入口保持可聚焦。
4. 路由切换只滚动当前菜单进入可见区，不主动夺取页面焦点；折叠入口和分组按钮具备中文 `title`、`aria-label` 与稳定 `aria-controls`。

## 业务阅读与精确上下文

1. 主阅读层优先显示中文业务名称、业务状态、影响与下一步，原始 ID、修订、哈希、枚举和 API 路径进入关闭状态的“审计详情”。
2. 禁用动作统一说明当前原因、缺失条件、责任入口与可执行动作，不把内部实现缺口包装成产品终态。
3. exact 上下文统一携带 `task/run/agent/logic/evidence`、修订、authority、cutoff 与安全 `returnTo`；非法引用失败关闭，不猜测或补造。

## 自动化回归

- Web 全量：`268/268` 测试文件、`2350/2350` 用例 GREEN。
- 生产构建：GREEN。
- 补丁格式：`git diff --check` GREEN。

## 安全边界

本波仅修改前端共享壳、业务阅读合同、精确上下文和相应用例；未执行 Provider 调用、发布、客户触达、改价、业务写入、迁移、真实外部副作用或 Release。
