# 八页菜单组件 · 视觉稿 · 数据功能 浏览器只读审计（2026-09-04）

- 依据：`WORKSHOP-EIGHT-MENU-PRODUCT-DEFECT-REMEDIATION` 清单 §3 统一验收定义（视觉三档视口成对截图 / 功能产生真实业务结果 / 数据可回溯同租户 exact ref）
- 租户：`org-org/dev-project`（栖月汇微商城）
- 视口：`1280×720`、`1440×900`、`1920×1080`
- 通道：CDP 只读。仅导航、展开、容器内滚动（滚动后复位）与截图；未点击写入按钮、未提交表单、未改主题/租户/折叠等持久化状态、未调用 Provider
- 基线对照：2026-08-30 审计结论（八页各自硬编码缩减菜单、每页仅 13 个链接；Buddy 完整 canonical 菜单 80 个链接）

## 1 菜单组件缺陷复验：已修复

`menu-container-audit.json`，`nav.nav`（`aria-label="主导航"`）逐页实测：

| 页面 | 侧栏主导航链接 | 滚动容器 | 可滚动 | 滚到底 | 末项在底部可见 |
| --- | --- | --- | --- | --- | --- |
| 日常任务总控大屏 | **80** | 有 | 是 | 是 | 是 |
| 内容与活动工作台 | **80** | 有 | 是 | 是 | 是 |
| 统一运营驾驶舱 | **80** | 有 | 是 | 是 | 是 |
| 达人邀约驾驶舱 | **80** | 有 | 是 | 是 | 是 |
| 多媒体内容生产 | **80** | 有 | 是 | 是 | 是 |
| 经营参谋 · 增长指挥中心 | **80** | 有 | 是 | 是 | 是 |
| 价格治理驾驶舱 | **80** | 有 | 是 | 是 | 是 |
| 客户关系工作台 | **80** | 有 | 是 | 是 | 是 |

结论：清单 §1 第 1 条（八页只有 13 个链接、未渲染完整菜单源）与第 2 条（折叠/展开的是错误的缩减菜单）**已复验修复**。八页侧栏主导航均为 80 项完整 canonical 菜单，与 Buddy 一致，滚动容器存在且可滚到底、末项完整可见。八页均无页面级水平滚动（三档视口 `horizontalOverflow = false`）。

## 2 数据接入复验：统一运营已从阻断切片变为真实计数

清单 §1 第 4 条指「统一运营已有 210 条可附着记录，但页面仍优先展示阻断切片或空的 Business Investigation Case」。实测（`03-operations-1280x720.png`）：

- KPI 带：业务视图 `7`、读取可用 `7`、待补条件 `0`、来源总量 `219`、已挂接 `210`、待核对 `9`、数据截止 `2026/9/4 10:31:58`
- 业务切片逐项真实计数：订单 `50`、订单明细 `50`、库存 `41`、履约 `19`、支付 `50`、售后事件 `0`
- 页脚 exact 归属：`租户 org-org/dev-project · 来源 219 · 已挂接 210 · 未匹配 9 · 冲突 0 · 无后续页：是`

结论：该条数据缺陷**已复验修复**，210 条已挂接记录与 219 来源总量已正确呈现并可回溯到同租户。

## 3 视觉稿成对对账：统一运营信息结构仍与基线不符（R4-O01 未做）

正确基线为 `foundry/html/workshop-app-order.html`（清单 §1 第 7 条明确不得用 `workshop-cop.html`）。成对截图 `03-operations-1280x720.png` ↔ `03-operations-VISUAL-1280x720.png`：

| 区域 | 视觉稿基线 | 当前实页 | 差异性质 |
| --- | --- | --- | --- |
| KPI 带 | 待处理订单 7 / 库存预警 3 / 风险告警 2 / 今日已完成 85 / 平均处理时长 8min / 退款率 3.2%，右侧「客服专员 在线·自动处理已开启」 | 业务视图 7 / 读取可用 7 / 待补条件 0 / 来源总量 219 / 已挂接 210 / 待核对 9 / 数据截止 | **语义错位**：把数据管道就绪度当作经营驾驶舱指标 |
| 左栏 | 统一待办 12 项 + 筛选 chips（全部/订单 7/库存 3/告警 2）+ 紧急/高优分组 + 逐条业务卡（订单号、客户、金额、时间、严重度） | 六个数据切片计数，每项仅「业务视图 / 可读」 | **缺主列表**：无单条待办、无筛选 chips、无紧急度分组 |
| 中栏 | 单条订单详情（`#DD20260811-003 · 退款申请`）+ 客户/订单金额/下单时间/退款金额/退款原因/退款方式 + 退款处理操作层级（驳回 / 修改金额 / 批准退款） | 该切片的来源总量、已挂接、未匹配、冲突计数 + 数据截止 + 正式数据来源 | **缺详情区与处置层级**：无单条订单详情，无退款处理操作 |
| 右栏 | 客户档案（VIP3、累计消费 ¥12,860、历史退款率 0%、风险等级低）+ AI 处理建议（同意部分退款 ¥199 + 三条依据 + 一键采纳）+ 附加建议（发送安抚券） | 证据链·边界 + 专业贡献归因（原子技能/逻辑编排/数字同事 全部「待核对」）+ 运营动作建议 `0 / 6 可提交` | **缺建议侧栏**：无客户档案、无带依据的处置建议 |
| 底栏 | 今日订单 92 / 待处理 12 / 已完成 85 / 退款率 3.2% / 平均处理时长 8min / 客服专员今日处理 67 件 / 最后同步 17:02 | 数据挂接账（来源/已挂接/未匹配/冲突/分页） | **语义错位**：经营结果 → 数据挂接 |

结论：R4-O01 的五项要求（订单/告警主列表、详情区、证据/建议侧栏、退款处理操作层级、正确视觉基线对账）**均未开始**，与清单中 `R4-O01` 未勾选一致。当前页面的失败关闭是诚实的（「当前视图没有可验证的数字同事运行记录，因此不制造运行事实」、`0 / 6 可提交`），不属于伪成功。

## 4 八页结构与交互信号（1280×720）

`eight-page-audit.json`：

| 页面 | H1 | h2/h3 | 按钮（禁用） | 页签 | 列表项 | 安全预检文案 |
| --- | --- | --- | --- | --- | --- | --- |
| 日常任务总控大屏 | 电商日常任务总控大屏 | 19/7 | 45（0） | 0 | 63 | 无 |
| 内容与活动工作台 | 内容与活动工作台 | 6/3 | 39（2） | 3 | 20 | 有 |
| 统一运营驾驶舱 | 统一运营驾驶舱 | 6/6 | 37（2） | 0 | 14 | 有 |
| 达人邀约驾驶舱 | 达人邀约与签约驾驶舱 | 10/3 | 38（2） | 0 | 18 | 无 |
| 多媒体内容生产 | 多媒体任务全过程闭环工作台 | 5/6 | 26（1） | 3 | 27 | 有 |
| 经营参谋 · 增长指挥中心 | 经营参谋 · 增长指挥中心 | 6/0 | 32（0） | 8 | 20 | 无 |
| 价格治理驾驶舱 | 价格治理驾驶舱 | 6/5 | 26（2） | 3 | 15 | 无 |
| 客户关系工作台 | 客户关系工作台 | 7/1 | 27（2） | 4 | 16 | 无 |

- 任务总控（已完成 R3-01～R3-07）结构显著丰富于其余七页（19 个 h2、63 个列表项），其余七页仍在 5～10 个 h2、14～27 个列表项量级，与 R4～R7 未开工一致。
- 内容与活动、统一运营、多媒体三页仍出现「安全预检」类文案，对应清单 §1 第 3 条尚未闭合的部分。
- 八页均出现可信空态词（暂无/没有/待核对/尚无/未就绪），未见伪造业务数字。

## 4b AV02 日常任务总控大屏 逐区域回归对账（R3-01～R3-07 全部已交付）

`av02-cockpit-region-audit.json`。判据取自清单 R3-01 精确施工范围的 1280×720 目标几何，并在三档视口同时实测。

### 几何：三档视口全部符合，且提前偿还 R9 响应式欠账

R3-01 §5 曾记录「当前内置浏览器不提供任意调整视口的接口……响应式多尺寸复验统一保留到 R9 累计验收」。本次经 CDP 设视口实测：

| 区域 | R3-01 目标（1280×720） | 1280×720 | 1440×900 | 1920×1080 | 判定 |
| --- | --- | --- | --- | --- | --- |
| 执行组窄栏宽 | 120px | 120 | 120 | 120 | 符合，三档恒定 |
| 策划组窄栏宽 | 120px | 120 | 120 | 120 | 符合，三档恒定 |
| 共享能力带高 | 50px | 50 | 50 | 50 | 符合，三档恒定 |
| 明日预告高 | 34px | 34 | 34 | 34 | 符合，三档恒定 |
| 侧栏可用高 | 随视口 | 424 | 604 | 784 | 随视口线性增长，未溢出 |
| 页面水平溢出 | 无 | false | false | false | 符合 |
| 页面纵向裁切 | 无 | 0 | 0 | 0 | 符合 |

R3-07 的遮挡修复原仅在 1280×720 验证，本次三档视口复测「共享能力带 × 明日预告」「右侧复盘 × 共享能力带」重叠均为 `false`，**遮挡修复在三档视口均成立**。R9 的响应式复验就该页几何部分可提前视为已有证据。

### R3 八项可见性复核（以截图为准，非选择器）

首轮脚本对 R3-02/R3-03/R3-05 返回 `false/0`，经 `01-cockpit-1280x720.png` 人工复核确认为**选择器缺陷，不是产品回归**，现更正如下：

| 项 | 实测 | 说明 |
| --- | --- | --- |
| R3-01 KPI 顶栏 | 在 | 7 项真实指标：今日经营任务 4 / 执行中 3 / 已锁定执行门 0 / 待补条件 0 / 待规划 1 / 需要补充数据 2 / 评估截止 10:31 |
| R3-02 推荐任务 | 在 | 下达带右侧三枚推荐 chip：核对经营数据异常、复盘订单与商品规模、梳理客户运营机会（脚本按「推荐」字面匹配，页面未用该字样，故误判） |
| R3-03 任务指令闭环 | 在 | 输入框「描述业务任务需求，系统将先做安全预…」+「下达」按钮（脚本 `input[type=text]` 未匹配到该实现，故误判） |
| R3-04 六数字同事 | 在，计数 6 | 执行组 客服专员/私家管家/导购顾问；策划组 数据参谋/内容官/活动策划师 |
| R3-05 共享能力标签 | 在 | 底部「共享技能 AGENT」带：素材采集、策略规划、文案生成、脚本撰写、语音合成、视频合成、内容审核…仅「策略规划」标可用，其余诚实标「暂不可用」 |
| R3-06 右侧三段 | 在 | 今日复盘 / AI 改进建议 / 经验沉淀 Wiki·今日入库，顺序正确 |
| R3-07 明日预告 | 在 | 「明日预告 · 仅显示已挂载业务任务」+「1 项待规划」 |

### 数据诚实性：空态不伪造

右侧三段当前均为空集合，且文案明确拒绝回退示例：「今日没有可回读的终态任务」「今日没有可回读的改进观察 · 不复制视觉稿示例建议」「今日没有入库记录 · 不复制视觉稿示例 SOP 条目」。中央任务流有 4 条真实业务任务（复盘栖月汇商城的 57 个商品／5 条商品…、经营任务自动化·受控运行 ×3），带「待规划／已批准」状态与「待核对 第 1 版」履历记录。符合清单 §3 第 3 条不得用演示数据补空。

### AV02 未测项

- 功能维度未测：未点击「下达」「重新读取」及推荐 chip 回填，属写入/交互闭环，超出本次只读边界。
- 视觉稿像素级对账未做：本次为区域几何与组件在位性对账，未做与 `workshop-task-cockpit.html` 的逐像素间距／字号／颜色比对。

## 4c AV03 达人邀约驾驶舱 逐区域成对对账（R5-D01～D05 全部未开工）

成对截图 `04-creator-growth-1280x720.png` ↔ `04-creator-growth-VISUAL-1280x720.png`，基线 `foundry/html/workshop-creator-outreach.html`。

| 区域 | 视觉稿基线 | 当前实页 | 差异性质 |
| --- | --- | --- | --- |
| 业务页签 | 达人库 `147` / 招募漏斗 / 商务签约 / 履约效果 / 长期关系，带计数徽章 | 同五个页签，**无计数徽章** | 结构在位；徽章缺失是 0 数据的必然结果，非独立缺陷 |
| 左栏 | 达人\|机构 切换 + 昵称/平台ID 搜索 + 三行筛选（平台/层级/状态）+ **逐条达人卡**（张小雨 抖音·核心 128.5万粉丝 合作3次 GMV ¥18.2万 等 6+ 条） | 一行筛选（全部达人/合作状态/内容领域）+ **四个阶段计数卡**（候选/邀约准备/合同/履约，均 `0 条正式数据`） | **缺主列表**：达人主列表 → 阶段管道计数；缺达人\|机构切换与两行筛选 |
| 中栏 KPI | 达人总数 147 / 核心合作 12 / 成长中 35 / 待邀约 86 / 逾订·冷启 2 / 累计 GMV ¥86.万 +12.3% 环比 | 无经营 KPI 带，仅「当前业务阶段 候选 + 等待数据」徽章 | **缺经营 KPI 带** |
| 中栏详情 | 达人详情（头像、粉丝、层级）+ 写邀请信/新建合作 + 达人画像七字段（平台身份已核验、所属机构星耀传媒 MCN、擅长品类、受众画像、开播频率、来源、入池时间）+ 合作历史表（日期/商品/类型/GMV/ROI/结果） | 「达人档案」空态 + 四格（基础画像 未挂接 / 权益结构 待核对 / 内容表现 待核对 / 合作记录 0）+ 输入0 合格0 待复核0 未知0 | **缺详情区与合作历史**，无任何单条达人 |
| 右栏 | 匹配评分 `92/100` + 建议合作方式（专场直播 ROI 预估 3.5x+、佣金 20-25%）+ 注意事项（档期近满、退货率 3.2%）+ 四条下一步建议动作 + 共享技能 Agent 编排 | 「当前没有可回链建议」+ 两个禁用按钮（读取达人档案／读取分析建议）+ 决策摘要「缺少正式数据依据，当前阶段保持可信空」 | **缺带依据的建议侧栏** |

### 根因与统一运营不同

统一运营已有 219 来源 / 210 已挂接的真实记录，缺的是前端信息结构；达人页**连正式数据都为 0 条**，根因在下层 canonical authority 未承接，对应清单 §1 第 5 条「达人等页面仍缺 canonical authority 或业务投影」。因此 R5-D 的施工顺序应是 `R5-D02`（Candidate/Outreach/Contract/Delivery/Relationship 的 authority、Store、reader 与同租户关联）先行，而非先补前端。

### 对 R5-D 各项的精确起点

| 项 | 本次实测起点 |
| --- | --- |
| `R5-D01` 五业务标签/阶段 + 三栏工作区 | **结构已部分在位**：五页签、三栏、四阶段管道均已渲染；缺的是左栏主列表形态、中栏 KPI 带与详情区 |
| `R5-D02` canonical authority/Store/reader | **未做**，0 条正式数据，是其余各项的前置 |
| `R5-D03` 导入/新建批次/编写邀约/审批闭环 | **未做**，右栏两个动作按钮为禁用态 |
| `R5-D04` 中文业务数据映射 + 缺数据补录入口 | **未做**，当前只有等待提示，无可执行补录入口 |
| `R5-D05` 三视口逐组件自验收 | **未做** |

### 诚实性加分项

实页明确拒绝伪造与越权披露：「不从视觉稿、缓存或模型补齐姓名与经营指标」「无可验证候选、内容表现或合作证据前，不生成匹配分、经营判断或联系方式」「客户敏感信息只显示受控引用；页面不复制联系方式、合同正文、地址或账号密码」。属正确失败关闭与隐私边界，不计为缺陷。

## 4d AV04 多媒体内容生产 逐区域成对对账（R5-M01～M06 全部未开工）

成对截图 `05-media-studio-1280x720.png` ↔ `05-media-studio-VISUAL-1280x720.png`，基线 `foundry/html/workshop-media-studio.html`。

| 区域 | 视觉稿基线 | 当前实页 | 差异性质 |
| --- | --- | --- | --- |
| 顶部上下文条 | 栖月汇·微商城 / 内容官 `(owner)` / 场景 `七夕活动专场 · 2026-08-15 开卖 · D-5` / 今日计划 `8 篇文案 + 12 条视频 + 2 场数字人直播` | 栖月汇微商城 / 内容官 `(负责人未绑定)` / 场景 `未绑定` / 今日计划 `待核对` | **owner 与场景未绑定** |
| KPI 带 | 今日文案产出 `8`（草稿6·已通过2）/ 视频成片 `12/16`（投产比 3.2x）/ 数字人直播 `1/2`（在线 1,247）/ 整体 GMV 贡献 `¥186,420`（较昨日 +24.6%） | 四项全部 `待核对`；前三项注 `数据来源未闭合`，第四项注 `当前合同未提供该指标` | **数据缺口 + 合同缺口**（第四项是合同未定义，性质不同于前三项） |
| 媒体类型页签 | 种草文案 `2/8` / 短视频 `3/12` / 数字人直播 `1` | 三个页签在位，均标 `等待条件`，无计数 | 结构在位，计数缺失 |
| 左栏任务列表 | 文案任务 + `新建` + 搜索（商品名/SKU/关键词）+ 筛选 chips（全部/草稿6/审核中1/已通过2/渠道就绪1）+ 四张任务卡（含商品、价格、渠道、分发对象、时间） | 「当前没有可审阅文案任务 · 生产上下文未闭合，不用演示素材补齐任务列表」+ 查看数据审计信息 | **缺任务列表、新建、搜索与筛选** |
| 中栏生产中心 | 内容详情（七夕专题 banner 文案 5 套）+ 查看历史版本/导出/批准发布 + **生产轨迹**（STANDARD、冻结输入 Brief r4·Evidence r7·Eval r5、CP-04、Draft-only、责任覆盖 6/6）+ **6 件事自动触发判定 5/6 命中** | 「等待生产上下文」+ 业务模块 `等待条件·暂无可验证来源` + 生产能力 `等待条件` | **缺详情、缺生产轨迹、缺 6 件事判定**，是该页信息密度差距最大处 |
| 右栏 | 内容官建议（5 套已生成/4 套过审/D 版功效宣称被打回）+ 历史同款 CTR `0.078` vs 行业 `0.045` + 合规提醒（小红书 8/15 新规）+ **投放决策 G6 受控 ACTION 预告**（朋友圈→私域 L2、详情页→上架 L2） | 「尚无可回链内容建议」+ 所需条件与下一步 `▶补充正式生产数据` + 重新读取 | **缺建议、缺合规提醒、缺 G6 受控 ACTION 分级预告** |

### 对 R5-M05 的定向判定

`R5-M05` 要求「空数据时允许创建可工作的内部草稿/计划，不得只显示 `MEDIA_LIFECYCLE_AUTHORITY_NOT_AVAILABLE` 而没有修复入口」。实测：该页**未**裸露 `MEDIA_LIFECYCLE_AUTHORITY_NOT_AVAILABLE` 字样，且提供了 `▶查看数据审计信息`、`▶补充正式生产数据`、`重新读取` 三个入口，比原始缺陷描述有改善；但这三个入口均为**说明/重读性质**，仍不构成「创建可工作的内部草稿/计划」。判定：`R5-M05` **部分改善，未满足**。

### 根因归类

与达人页同属「下层未承接」，但多媒体页多一层：`整体成交贡献` 明示 `当前合同未提供该指标`，属**合同缺口**而非数据缺口，需在 `R5-M02`（媒体生命周期 authority/reader）中先补合同字段，不能靠补数解决。

诚实性正确：「不用演示素材补齐任务列表」「未形成可审阅内容、版本或交付结果」「建议必须来自可验证数据与同一截止面的证据」。

## 4e AV05 经营参谋 · 增长指挥中心 逐区域对账（R6-01～R6-09 全部未开工）

截图 `06-analyst-1280x720.png`；同源同载取证 `av05-analyst-ready-vs-label.json`、`av05-analyst-raw-response.json`。

### R6-01 缺陷复现（可直接判定）

`R6-01` 要求「将渠道视觉绑定到真实『栖月汇微商城』，不再显示『渠道未知/未选择经营实体』」。实测页面文案原样存在：

- `渠道视角` → `渠道未知`
- 状态条 → `渠道未选择 · 数据截止未验证`
- `经营实体` → `未选择经营实体`

判定：`R6-01` **未做，缺陷复现**。

### 页签状态与接口一致性：无冲突（同一次页面加载取证）

清单 §2.2 第 4 条禁止用不同快照拼同一结论，故本项在**同一次 `/workshop/analyst` 加载**内同时捕获接口响应与页签文案：

| 视图 | 接口 ready 指标 | 页面页签 | 判定 |
| --- | --- | --- | --- |
| 经营总览 overview | 2/2 ready | `经营总览可读取` | 一致 |
| 驱动因素 drivers | 0/2 ready | `驱动因素等待条件` | 一致 |
| 问题诊断 diagnosis | 0/2 ready | `问题诊断等待条件` | 一致 |
| 增长计划 plan | 0/2 ready | `增长计划等待条件` | 一致 |
| 效果复盘 effects | 0/2 ready | （页签存在，标签映射未命中） | 未判定 |
| 证据链 evidence | 0/2 ready | `证据链等待条件` | 一致 |
| 数据质量 quality | 2/2 ready | `数据质量可读取` | 一致 |

**冲突数 0**。页面没有把可读取视图误标为阻断，也没有把阻断视图伪装成可读，属正确呈现。

### 基线漂移待复验（不计为缺陷）

同一接口 `/api/v1/ecommerce-workshop/views/analyst` 在本会话早前的只读观察 `2026-09-04-r3-cockpit-layout-and-task-loop/canonical-reads-report.json` 中曾返回 **12 个指标全部 ready**，含 `drivers: product_count 57 / product_review_count 5 / customer_count 54`、`diagnosis: order_count 124 / order_line_count 234 / customer_count 54`、`quality: ready_source_count 10 / failed_source_count 2`。本次同载观察中 drivers 与 diagnosis 均为 `0/2 ready`。

两次为不同快照，按 §2.2 第 4 条不得拼接。记为**基线漂移，待稳定版复验**，需要在同一截止面重新取证后才能判断是数据回退、租户上下文差异还是采集时序差异。该差异同时影响 `R6-02`（order 124 / product 57 / customer 54 水合）的起点判断，施工前必须先复验。

### 其余区域现状

- 三阶段推进（①经营画像 ②问题与机会 ③方案设计）结构在位，均标 `待进入 · 需要正式分析记录`，对应 `R6-05` 未做。
- 经营分析记录为 `可信空`，边界文案「当前租户没有正式分析记录；页面仅呈现可信空工作区，不注入演示经营事实」「未知或未创建不能显示为 0，也不以演示记录补齐」，符合 `R6-07` 非因果与不伪造要求。
- 依赖与下一证据显示 `数据读取 0 · 分析服务 0 · 外部操作 0`，`业务操作: 只读`，无外部副作用。
- 八个页签（生意探究/经营总览/驱动因素/问题诊断/增长计划/效果复盘/证据链/数据质量）**结构已全部在位**，`R6-04` 的标签部分已具备，缺的是各标签下的实现与状态。

## 4f AV06 价格治理驾驶舱 现状对账（R7-P01～P05 全部未开工）

截图 `07-price-governance-1280x720.png`。

- **R7-P01 结构部分在位**：三页签（同款价格治理 / 竞品比价 / 调度控制台）均已渲染，但全部标 `等待条件`；左栏四区（核心商品列表 `待核对`、异常候选 `未挂接`、待复核商品 `未挂接`、可比报价 `待核对`）在位但无数据；缺视觉稿的指标带、异常列表与竞品卡。
- **中栏价格治理任务链六卡在位**：采集证据 / 同款判定 / 策略与业务事项 / 通知就绪 / 建议交接 均 `等待条件 · 暂无可验证来源`；自动调价 `待核对 · 专业调价能力未开启`。
- **R7-P03 门控正确**：`自动调价` 明示能力未开启，未提供可直接触发真实调价的路径，符合「真实抓取/调价保持门控」。
- **R7-P04 未满足**：右栏仅 `▶补充正式价格数据` 说明入口，未提供计划/补录的可工作路径，仍接近「死空态」。
- 右栏数字同事协作诚实：「数据参谋主责 · 正式数据待挂接；活动策划师与导购顾问尚未确认调整，不生成价格结论」。
- 顶部状态条出现 `3 个视图待复核`，与三页签 `等待条件` 自洽。

## 4g AV07 客户关系工作台 定向判定（R7-C01～C05）

截图 `08-customer-1280x720.png`。本页有清单中最可判定的一条判据。

### R7-C03：已显著改善，基本满足

`R7-C03` 原文要求「现有 CustomerLite 输入 54 条时，至少展示合法汇总、未知/排除计数和修复入口；不能让四个视图全部显示 0 且无可操作路径」。清单 §1 第 5 条原缺陷为「客户源已有 54 条 CustomerLite 输入，但 4 个业务视图均为 0」。实测：

| 判据 | 实测 | 判定 |
| --- | --- | --- |
| 四视图不得全部显示 0 | 客户最小投影 / 客户分群 / 生命周期旅程 / 对话与批次 四项均标 `等待条件 · 正式来源 54`，**未显示 0** | 满足 |
| 展示合法汇总 | 「已读取正式来源 54 条；允许披露 0 条，54 条因同意、目的或留存条件未闭合而不展示客户明细」 | 满足 |
| 未知/排除计数 | 已读取 54 / 允许披露 0 / 因 consent·purpose·retention 排除 54，三个数字齐备且自洽 | 满足 |
| exact ref | 客户最小集标 `可读取`，带 `CustomerLiteSourceWindow · r29808060` | 满足 |
| 修复入口 | 右栏五条「补充同租户、同数据截止的客户最小化正式数据」+ `▶查看审计状态码` | 入口存在，**可执行性未测** |

判定：`R7-C03` **基本满足**，是本轮八页中相对原始缺陷改善最明确的一项。保留项：修复入口为说明性入口，是否可执行需点击验证，属写入/交互边界，本次未测。

### 其余项现状

- `R7-C01` 结构部分在位：四视图 + 中栏六卡（客户最小集/同意依据/分群投影/旅程投影/对话摘要/触达批次）+ 右栏条件区；缺视觉稿的名单区与客户详情区。
- `R7-C02` 未做：同意依据、分群投影、旅程投影、对话摘要、触达批次均 `等待条件 · 暂无可验证来源`。
- `R7-C05` **正向证据**：页面在有 54 条真实来源的前提下仍坚持 `允许披露 0 条`，并以 consent/purpose/retention 三类条件说明排除理由，未泄露任何客户明细，符合脱敏与目的限制要求。

## 5 本次审计的边界

- 只做了只读观察。**未验证任何写入闭环**：按钮是否产生真实工作台内部业务结果（清单 §3 第 2 条）需要点击写入动作，本次未做，标记为**未测**。
- 视觉对账本次只完成统一运营一页的逐区域成对分析；其余七页已采集三档视口成对截图但未逐区域出结论，标记为**未测**。
- 视觉稿为静态 mockup（div 结构，无语义 `h2`/`li`），因此 `visual-source-audit.json` 的 DOM 计数不能与实页直接相减，只能作为截图配对索引。
- 本审计不改变任何门禁状态，不更新 authority、Lease、Receipt 或 Prime，不勾选清单任何项。

## 5b 审计 Loop 断点（2026-09-04 10:54 暂停）

暂停原因：用户额度受限，按其要求在 AV07 完成后暂停。

| 波次 | 页面 | 状态 | 提交 |
| --- | --- | --- | --- |
| AV01 | 统一运营驾驶舱 | 6/6 已封板 | `689c3e87` |
| AV02 | 日常任务总控大屏 | 6/6 已封板 | `b5a5fb90` |
| AV03 | 达人邀约驾驶舱 | 6/6 已封板 | `51ca3165` |
| AV04 | 多媒体内容生产 | 6/6 已封板 | `b7f26343` |
| AV05 | 经营参谋 · 增长指挥中心 | 6/6 已封板 | `66a58668` |
| AV06 | 价格治理驾驶舱 | 6/6 已封板 | 本次提交 |
| AV07 | 客户关系工作台 | 6/6 已封板 | 本次提交 |
| AV08 | 内容与活动工作台 | **未进入** | — |

### 接续条件

1. **AV08 阻塞原因**：`R4-C01 WORKSHOP-R4-C01-CONTENT-CAMPAIGN-VISUAL-STRUCTURE` 租约由 `aos-main-development` 持有，`ACTIVE`，`expires_at 2026-09-04T16:00:00+08:00`。该页正在施工，现在对账拿到的是漂移基线。**必须等对方封板后再进入 AV08**，接续前先重读 `authority.json` 与 `leases.json`。
2. **接续前必须复核**：`project_revision`（暂停时 `AOS-000464`）、`next_gate`（暂停时 `WORKSHOP_R4_C01_CONTENT_CAMPAIGN_VISUAL_STRUCTURE`）、`last_green_gate`（暂停时 `WORKSHOP_R3_07_COCKPIT_LAYOUT_TASK_LOOP_GREEN`）、m1 HEAD、Vite 服务 `127.0.0.1:5173` 可用性、CDP 端口可用性。
3. **遗留待复验项**：AV05 记录的 analyst 接口基线漂移（早前 12 指标全 ready，同载观察 drivers/diagnosis `0/2 ready`），需在同一截止面重新取证后才能作为 `R6-02` 施工起点。
4. **两个维度整体未测**：清单 §3 第 2 条功能维度（按钮产生真实业务结果）需写入闭环授权；像素级间距/字号/颜色对账未做。八波结论均只覆盖视觉结构与数据两个维度。
5. **不得重放**：本审计全程只读，未创建任何 Task/Receipt，接续时无需回滚，也不得把已封板波次的结论迁移到新版本页面。

## 6 产物

- 实页截图：`0N-<page>-<viewport>.png`（8 页 × 3 视口 = 24 张）
- 视觉稿截图：`0N-<page>-VISUAL-<viewport>.png`（8 页 × 3 视口 = 24 张）
- `eight-page-audit.json`、`menu-container-audit.json`、`visual-source-audit.json`
- 脚本：`audit-eight-pages.mjs`、`probe-menu-container.mjs`、`capture-visual-sources.mjs`
