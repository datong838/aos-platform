# WORKSHOP-R3-07 任务总控布局与真实任务回读验证

- 任务：`WORKSHOP-R3-07-COCKPIT-LAYOUT-TASK-LOOP`
- 租户：`org-org/dev-project`（栖月汇微商城）
- 页面：`/workshop/cockpit`
- 边界：只读验证，不新增业务任务，不触发发布、发送、退款、调价、Provider 或其他外部副作用。

## 专项与累计回归

- `npx vitest run src/components/workshop/TaskCockpitPage.test.tsx`：31/31 通过。
- `npm test`：271 个测试文件、2436 项测试全部通过。
- `npm run build`：TypeScript 与 Vite 生产构建通过；首次清理旧 `dist` 遇到系统废纸篓 `-5000`，将旧目录完整迁移到 `/private/tmp` 后同一源码构建通过，未删除用户文件。

## 内置浏览器交互

1. 经营参谋推荐「复盘订单与商品规模」点击后，任务输入框自动填入由 canonical 指标生成的中文栖月汇业务任务。
2. 状态筛选切到「已完成」得到 0 项，恢复「全部状态」回读 4 项；空态与回读计数一致。
3. 「导购顾问」介绍浮层可打开并显示专业能力、工作边界、常用 Agent 与可信当前状态；关闭后页面内容保持。
4. 「策略规划」能力贡献链可打开，显示 CapabilityRevision、Skill、Logic、数字同事与任务总控贡献。
5. 右侧固定显示「今日复盘」「AI 改进建议」「经验沉淀 Wiki · 今日入库」，无视觉稿示例数据注入。

## 三视口与溢出几何

- 1280×720、1440×900、1920×1080 三视口中，任务板、能力带与明日预告严格相邻，无重叠；六位数字同事完整存在；页面外层不滚动。
- 在浏览器中临时克隆现有任务卡形成 12 卡溢出，只用于 DOM 几何验证并在同一表达式内恢复：任务列 `clientHeight=424`、`scrollHeight=1022`、可滚动至 `scrollTop=598.5`；数字同事列保持 `scrollTop=0`，能力带与明日预告坐标不变，外层仍不滚动；恢复后 canonical 任务仍为 4 项。
- 详细结构化结果见 `built-in-browser-report.json`。

## 方案一致性与风险

- 与 R3-07 精确施工范围一致：底部两带参与 Flex 正常流；三列独立滚动；真实租户完成推荐填充、筛选、浮层、能力贡献和复盘回读；创建链由 R3-03 专项回归继续覆盖。
- 本波未修改生产实现，因为现有 CSS 与组件已通过当前数据、溢出场景和三视口验收；避免无目的改动造成回归。
- 风险保持：浏览器没有为布局压测新增真实业务数据；真实新增任务仍由既有内部任务创建契约、权限、Receipt 与审计链控制。
