# W2 · A2 模块接口 schema CRUD 方案

> 版本：v1.0（2026-07-29）  
> 目标：`/workshop/module-interface` 45% → 65%  
> 约束：最小改动；不碰 Variables / Draft / Canvas / nav

## 1. 范围

| 项 | 做法 |
|----|------|
| 页面 | **新建** `apps/web/src/pages/s2/ModuleInterfacePage.tsx`，从 `extras.tsx` 迁出完整逻辑并补 CRUD |
| re-export | `extras.tsx` 仅保留 `export { ModuleInterfacePage } from "./ModuleInterfacePage"`，routes 继续从 extras 懒加载，**不动 routes** |
| API | 复用 `GET/PUT /v1/modules/:id/interface` |
| 后端 | `module_interfaces.py` / `modules_interface.py` 仅补字段规范化（`name`/`key`/`direction`） |
| CSS | `styles.css` 末尾追加 `/* === W2-A2 === */` 编辑表样式 |

## 2. 数据模型

前端编辑行：`{ id, name, type, direction: "input" | "output" }`

持久化映射（兼容现有 seed：`{ key, type, required }`）：

- 写入：`entryParams[]` 每项 `{ name, key, type, direction, required? }`
- 读取：无 `direction` → 默认 `input`；无 `name` 则用 `key`
- 可选：PUT body 另带 `outputParams` 时，后端合并为 `direction=output` 写入同一 `entry_params`（无新列）

`expose` / `name` / `description` / `version` 原样读写，不破坏现有测试。

## 3. UI 行为

1. 列表：`GET /v1/modules` 选中模块  
2. 加载：`GET /v1/modules/:id/interface` → 填入可编辑表  
3. 增删改：本地 state；保存 `PUT` 全量 `entryParams`  
4. 失败：降级 MOCK 字段 + 角标「演示路径 · MOCK」  
5. 保留：嵌套 Loop 示意、模块列表、创建 Module / Runtime（既有能力不删）

## 4. 验收

- [ ] 可增删改 input/output（name/type/direction）并 PUT 持久化  
- [ ] 刷新后 GET 回显一致  
- [ ] API 失败可见 MOCK 降级标识  
- [ ] routes 仍可打开 `/workshop/module-interface`  
- [ ] 既有 `TestModuleInterface` 不回归  

## 5. 风险

- 旧 seed 只有入参、无 direction → 读时默认 input，行为兼容  
- 出参与入参同存 `entry_params`，不改表结构，回滚成本低  
