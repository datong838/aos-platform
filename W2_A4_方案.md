# W2 · A4 分析师真查询路径方案

> 版本：v1.0（2026-07-29）  
> 目标：`/aip/analyst` 35% → 60%  
> 约束：最小改动；严格文件所有权；不碰 Observability/Capacity/ModelCatalog/Studio/Publish/Capability/nav

## 1. 范围

| 项 | 做法 |
|----|------|
| 后端新建 | `services/aos-api/aos_api/routers/aip_analyst.py`：`POST /v1/aip/analyst/query` |
| 可选引擎 | `services/aos-api/aos_api/aip_analyst_query.py`：SELECT 校验 + 内存表查询 |
| main.py | **仅** import + `include_router` 两处，注释 `# W2-A4` |
| 前端 | `AipAnalystPage.tsx`：运行按钮走 API；失败黄条「演示路径」+ 本地 MOCK |
| 测试 | 前端纯函数/映射单测；后端 query 路由/引擎单测 |
| CSS | `styles.css` 末尾追加 `/* === W2-A4 === */` |

## 2. API 契约

**请求** `POST /v1/aip/analyst/query`

```json
{ "sql": "SELECT ...", "naturalLanguage": "可选 NL" }
```

至少提供 `sql` 或 `naturalLanguage` 之一；两者皆有时以 `sql` 为准。

**响应**

```json
{
  "ok": true,
  "columns": [{ "name": "name", "type": "string" }],
  "rows": [{ "id": "s1", "name": "...", "coords": "...", "rating": 5 }],
  "durationMs": 12,
  "cacheHit": false,
  "source": "live"
}
```

- `source: live` — 命中内存表并执行（真查询路径）
- `source: fallback` — 无法解析/未知表时返回结构化样例（仍 200，供联调）
- 非 SELECT / 危险语句 → **400**

## 3. 引擎行为（最小）

内存表：`shops`（与前端 MOCK_RESULT 对齐）、`inventory`、`sales`。

1. NL → 简易映射为 SELECT（含「咖啡/Northampton/店铺」→ shops；「库存」→ inventory；「销售」→ sales；否则 shops）
2. 校验：trim 后必须以 `SELECT` 开头；拒绝 `;` 多语句、含 `INSERT|UPDATE|DELETE|DROP|ALTER|TRUNCATE`
3. 从表名路由到内存行；可选 `WHERE city = '...'` / `rating >= N` / `stock < N` 简易过滤
4. 列投影：解析 SELECT 列（`*` 全列）；保留 `id` 便于地图

## 4. 前端行为

1. 点击「运行」→ `apiPost('/v1/aip/analyst/query', { sql })`
2. 成功：`setResult`；`source === 'live'` 绿条「真查询」；`fallback` 黄条「演示路径」
3. 失败（网络/4xx/5xx）：黄条「演示路径」+ 本地 `MOCK_RESULT`（刷新 duration）
4. 非 SELECT：不发请求（沿用既有 `isSelectQuery`）
5. 初始仍可展示 MOCK，运行后按 API/降级更新

## 5. 验收

- [x] NL 或 SQL 至少一条路径返回 `source: live` 且前端展示行数据
- [x] API 不可达时黄条「演示路径」+ MOCK，页面不崩
- [x] main.py 仅两处注册改动
- [x] 不碰禁止文件
- [x] 相关单测通过（前端 59；后端需 PG 或 importlib 冒烟）

## 6. 风险

- 内存表非真实 DB：演示级 live，后续可换 PG 而不改契约
- NL 映射极简：复杂问句走 fallback，前端可标演示路径
- main.py 并发：仅追加两行，Planner 合并时注意冲突
