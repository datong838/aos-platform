# W2 · C2 对象类型详情方案（W3）

> 版本：v1.0（2026-07-29）  
> 目标：`/ontology/object-types/:id` 65% → 80%  
> 约束：最小改动；严格所有权；不碰 Wiki/Property/Function/pipeline/source/nav/main；禁止 push/m1/merge

## 1. 范围

| 项 | 做法 |
|----|------|
| 元数据 KV | Overview `BpPropGrid` 由 5 项扩到约 10–12 项（向视觉稿靠拢，不必 1:1） |
| 链接关系 | 替换纯 ASCII `<pre>` → 结构化列表 + SVG/CSS 节点边图；失败降级为空图/提示 |
| Tab/API | **保持**现有 7 Tab 与既有 list/link/action/module API |
| 后端 | 扩展已有 `GET /v1/ontology/object-types/{type_id}` 返回派生元数据；**不改** `main.py` |
| 前端 | `ObjectTypeDetailPage.tsx`、`objectTypeDetail.tsx`(+test) |
| CSS | `styles.css` 末尾追加 `/* === W3-C2 === */` |

## 2. 元数据项（目标）

| KV | 来源 |
|----|------|
| RID | 派生 `ri.ontology.main.object-type.{id}` |
| API 名 | `id` |
| PK | properties[0].name 或 `id` |
| TitleKey | properties 中 title/name 优先，否则次属性或 PK |
| 显示名 | `name` |
| Plural | 派生 `{name}s`（可被 API 字段覆盖） |
| BackingDataset | API 字段或 `ds/{id.lower()}` |
| Sync 策略 | API 字段或默认 `incremental` |
| 存储类型 | API 字段或默认 `object_storage` |
| 创建人 | API/`system` |
| 分支 | 前端 `branchId`（不入库） |
| 可见性 | `required_markings` 摘要或 `org` |

缺失字段一律用派生/默认，**不改表结构**。

## 3. 链接图

1. 过滤 `link-types` 中 `srcType|dstType === typeId`（已有逻辑）
2. 布局：中心节点 = 当前 OT；邻居环形分布；边标签 = `rel`
3. 渲染：SVG 线 + CSS 节点块；旁挂结构化列表
4. 无链接：提示「暂无 Link Type」；API 失败：空列表（既有 catch）

## 4. 验收

- [x] Overview 元数据 ≥10 项 KV，含 RID/PK/TitleKey/Backing 等
- [x] Links Tab / Overview 预览为非纯 ASCII 可视化
- [x] 现有 Tab 与保存元数据/分支逻辑不回归
- [x] 详情 API 失败时仍可展示列表派生元数据
- [x] 单测通过；commit `w2(C2): ...`；交付 `W2_W3_DONE.md`

## 5. 风险

- 派生字段非真实 Foundry 元数据，后续可换真字段而不改 UI 契约
- phase4 内存引擎与 PG `meta_object_type` 双轨并存；本波只扩 PG 详情 GET
- OntologyPage 内嵌面板自动受益（panel 自取详情），不改其文件

## 6. 回滚

还原 `get_object_type`  enrichment、前端图组件与 styles 附录即可。
