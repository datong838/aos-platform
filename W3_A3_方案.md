# W3 · A3 Draft 真审批链路方案

> 版本：v1.0（2026-07-29）  
> 范围：`/aip/drafts` 55% → 75%  
> 工作树：`aos-platform-w3` / `feature/223-worker-3`

## 1. 目标与验收

| 验收项 | 标准 |
|--------|------|
| 有后端 | 列表/详情/批准/驳回走真实状态（`listDrafts` + `approveDraft` / `rejectDraft`） |
| 无后端 | MOCK 降级，UI 标明「演示路径」 |
| 状态机 | 对齐 `draft/submitted/in_review/approved/rejected/withdrawn/changes_requested` |
| 写回后 | 批准/驳回后刷新列表与 timeline |
| 测试 | `DraftInboxPage.test.ts` 覆盖映射/状态机；后端引擎既有用例不破 |

## 2. 现状与约束

### 2.1 现状缺口

- 页已有三栏 UI + 本地状态机，但 **批准/驳回只改本地 MOCK**，未调 SDK。
- `listDrafts` 成功后映射过粗（空 summary/changes/timeline，type 固定 Action）。
- `apiItems.length === 0` 时回退 MOCK → **后端空列表也会被当成演示**，不正确。
- 引擎 `VALID_TRANSITIONS` 仅 `draft↔approved/rejected`，与前端 7 态不一致。

### 2.2 路由现实（只读事实，不改 main/drafts.py）

| 路径 | 实际优先处理方 | 备注 |
|------|----------------|------|
| `GET/POST /v1/aip/drafts` | Wave-3 `drafts.py` | 状态：`proposed/approved/rejected` |
| `POST .../approve` | `runtime_write.py` | 生产写回路径 |
| `POST .../reject` | Wave-3 `drafts.py` | |
| Phase3 `phase3_aip_drafts` | 同 path 后注册，**被遮蔽** | 仍强化引擎+响应形状，供单测与未来解耦 |

**结论**：前端真链路走 `@aos/ontology-sdk`（Wave-3）；Phase3 引擎对齐 7 态 + timeline，保持 `draft→approve/reject` 兼容既有单测。

### 2.3 文件所有权（可写）

- `apps/web/src/pages/DraftInboxPage.tsx`
- `apps/web/src/pages/DraftInboxPage.test.ts`
- `services/aos-api/aos_api/aip_drafts_engine.py`
- `services/aos-api/aos_api/routers/phase3_aip_drafts.py`
- `apps/web/src/styles-di-bridge.css`（仅追加 `/* W3-A3 */`）

禁止：Variables/Canvas/ModuleInterface/extras/nav/主 styles.css；push；改 m1。

## 3. 设计

### 3.1 数据源模式

```
sourceMode = "live" | "demo"
live  ← listDrafts() 成功（含空列表）
demo  ← 网络/鉴权失败 → MOCK_DRAFTS + 「演示路径」角标
```

不再用 `apiItems.length > 0` 判断。

### 3.2 API → UI 状态映射

| API status | UI DraftStatus | 说明 |
|------------|----------------|------|
| `proposed` | `in_review` | Wave-3 待审；露出批准/驳回 |
| `draft` / `submitted` / `in_review` / … | 原样 | Phase3 扩展态 |
| 其它未知 | `draft` | 安全降级 |

### 3.3 行映射（纯函数，可测）

`mapApiRowToDraftItem(row)`：

- id/title/status/submittedBy/时间戳
- `summary`：`content` / `proposed` 摘要 / 空串
- `changes`：来自 `changes` 字段，或由 `proposed` 键合成
- `timeline`：来自 `timeline`/`activity`，否则按 status **合成最小历史**（create → … → 当前态）
- `type`：`draft_type` / `actionTypeId` 映射到 4 种 DraftType，默认 `Action`

### 3.4 批准 / 驳回

| 模式 | 行为 |
|------|------|
| live | `approveDraft(id)` / `rejectDraft(id, { reason })` → 成功后 `reloadFromApi()`；失败设 `err` |
| demo | 既有本地 `transition()`，追加 timeline |

本地非终态操作（提交/进入审批/撤回/退回）在 **demo** 保持；**live** 下若当前为 mapped `in_review`（即 Wave-3 proposed），以批准/驳回为主路径。

### 3.5 后端引擎（最小增强）

- `VALID_TRANSITIONS` 扩至与前端一致的 7 态；**额外保留** `draft → approved|rejected` 以兼容 `test_phase3_aip_drafts.py`（该测文件不可改）。
- `DraftReview` 增加 `timeline: list[dict]`；approve/reject/reopen/transition 写 timeline。
- `transition(id, to, actor=, comment=)` 通用转换。
- 路由：`list`/`approve`/`reject` 响应增加 camelCase 别名 + timeline；可选 `GET /drafts/{id}`、`POST /drafts/{id}/transition`（不破坏既有签名）。

### 3.6 UI / CSS

- 顶栏：`live` →「真实 API」；`demo` →「演示路径」。
- bridge 追加 `.di-source-badge*`（`/* W3-A3 */`），不改主 `styles.css`。

## 4. 测试计划

1. `DraftInboxPage.test.ts`：状态机既有用例 + `mapApiStatus` / `mapApiRowToDraftItem` / 合成 timeline。
2. `pytest services/aos-api/tests/test_phase3_aip_drafts.py`：确认不回归。
3. vitest Draft 页相关用例全绿。

## 5. 风险与回滚

| 风险 | 缓解 |
|------|------|
| Wave-3 / Phase3 双路由遮蔽 | 前端以 SDK/Wave-3 为准；引擎对齐不依赖解遮蔽 |
| Wave-3 无 timeline | 合成最小 timeline；刷新后仍可见终态 |
| 扩大引擎转换破坏旧测 | 保留 draft→approve/reject 捷径 |
| 回滚 | 单 commit `w3(A3):`；`git revert` 即可 |

## 6. 非目标

- 不解 Wave-3 / Phase3 路由冲突（需改 main，越权）
- 不改 ontology-sdk / drafts.py / runtime_write.py
- 不做 226 视觉像素对齐
