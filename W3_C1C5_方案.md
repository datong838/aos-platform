# W3 · C1+C5 方案（Wiki 详情 + Wiki 差异）

> Worker W1 · 分支 `feature/223-worker-1` · 基线 `b7c8760`  
> 目标：C1 35%→55% · C5 40%→60% · 最小改动 · 不碰禁止文件

## 1. 验收对照（227）

| 项 | 要求 | 做法 |
|----|------|------|
| C1 主内容可编辑并保存 | 有明确主内容编辑区 + 保存 | 主内容 textarea；`PUT /v1/ontology/wikis/{id}` |
| C1 版本列表可见 | 页面展示版本历史 | `GET .../versions`；失败用 MOCK |
| C1 接已有 API | 不新建 pages 模型 | 复用 `phase4_ontology_wikis` |
| C1 失败降级 | 标「演示路径」+ 本地可继续 | `dataSource: live \| demo` + 黄条 |
| C5 两版本文本 diff | 可展示 | 本地 LCS `computeDiff`（已有） |
| C5 优先真 API | 有则用版本内容 | `GET .../versions`；可选 `GET .../diff` |
| C5 弱则本地 | API 失败仍可用 | MOCK_VERSION_CONTENTS |

**非目标**：完整 Slate 1:1、Object Set、工作流连线、改 `main.py` / `nav.ts`、碰他人页。

## 2. API 契约（已有，不改路由挂载）

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/v1/ontology/wikis/{id}` | 详情（title/content/version/author） |
| PUT | `/v1/ontology/wikis/{id}` | 保存（title/content/message）→ 新版本 |
| GET | `/v1/ontology/wikis/{id}/versions` | 版本列表（含 content） |
| GET | `/v1/ontology/wikis/{id}/diff?from_version=&to_version=` | 行级 added/removed（可选增强） |

前端旧路径 `/v1/ontology/wiki/...`（单数）作废，改为 `wikis`。

## 3. 前端改动

### 3.1 `WikiDetailPage.tsx`(+test)

- 导出映射纯函数：`mapApiWikiToPage` / `mapApiVersions` / `extractMainContent` / `buildWikiUpdateBody` / `localSavePage`
- 加载：先请求详情+版本；成功 `live`；失败 MOCK +「演示路径」
- UI：顶栏保存；**主内容**可编辑；版本时间线用 `versions` state
- 保存：PUT；成功刷新 version；失败本地 bump version + 演示路径文案
- 保留现有微件/工作流/预览三模式（不删），主内容与首个 text widget 同步

### 3.2 `WikiDiffPage.tsx`(+test)

- 加载 versions → 建成 `WikiVersionContent[]`；失败 MOCK
- Diff 渲染仍走本地 `computeDiff`（不依赖后端 diff 形状）
- 可选调用 API diff 填摘要；失败忽略
- 顶栏「演示路径」徽章（demo 时）

### 3.3 `styles.css`

- **仅末尾**追加 `/* === W3-C1C5 === */` 演示/live 徽章样式

## 4. 后端最小改（可选且限 wiki）

| 文件 | 变更 |
|------|------|
| `ontology_wiki_engine.py` | `diff()` 附带 `from_content`/`to_content`（便于前端优先 API） |
| `demo/seed_phase4_ontology_wikis.py` | 种子固定 id `wiki-covid-homepage` + 多版本，方便联调 live |

不改 `main.py`（router 已挂载）。

## 5. 风险与回滚

1. 种子 id 固定后与随机 id 并存，无破坏。  
2. 前端 body 从整页 WikiPage 改为 `{title,content,message}`，与后端 Pydantic 对齐。  
3. 回滚：还原两页 + styles 附录 + seed/engine 小改即可。

## 6. 自测

```bash
cd apps/web && npm test -- src/pages/s2/WikiDetailPage.test.ts src/pages/s2/WikiDiffPage.test.ts
cd services/aos-api && python3 -m pytest tests/test_phase4_ontology_wikis.py -q
```
