# W3 · W2 A6+A7 容量 + 模型目录方案

> 版本：v1.0（2026-07-29）  
> 范围：`/aip/capacity` 40%→60%；`/aip/model-catalog` 45%→65%  
> 工作树：`aos-platform-w3` / `feature/223-worker-3` / 基线 `ad2241a`

## 1. 目标与验收

| 项 | 标准 |
|----|------|
| A6 有后端 | 用量来自 `GET /v1/aip/capacity/usage`；项目/用户限流来自 `project-limits` / `user-limits` |
| A6 无后端 | 本地 MOCK + 顶栏 **「演示路径」** |
| A7 有后端 | 目录/已注册来自 `GET /v1/aip/model-catalog`（及 register / registered-models） |
| A7 无后端 | MOCK 降级 + 「演示路径」 |
| 约束 | 不改 `main.py` / `nav.ts` / 他人页面；`styles.css` 仅末尾 `/* === W2-A6A7 === */` |

## 2. 现状与缺口

### 2.1 CapacityPage
- UI 三 Tab（用量 / 速率限制 / 预留）齐全，数据全硬编码。
- 后端已有：`usage`（按日 items + summary）、`project-limits`（单条）、`user-limits`（**强制 userId**）。
- 缺口：前端未接线；`GET user-limits` 无列表态，无法填用户表。

### 2.2 ModelCatalogPage
- UI 四 Tab + 筛选/对比齐全，目录硬编码。
- 后端已有：`/model-catalog`、`/:id/register`、`/registered-models`、`/model-admin/models`（catalog∪registered）。
- 缺口：前端未接线；API 字段（displayName、数值价、capabilities 命名）与 UI 不一致。

## 3. 设计

### 3.1 数据源模式（两页共用）

```
sourceMode = "live" | "demo"
live  ← 主读接口成功（含空列表）
demo  ← 网络/鉴权失败 → MOCK + 「演示路径」角标
```

不以「空列表」判定演示（对齐 A3）。

### 3.2 A6 映射

| UI | API | 规则 |
|----|-----|------|
| today/week/month bucket | `usage.items[].day/totalRequests/totalTokens/cost` | 按日历聚合今日 / 近 7 天 / 近 30 天 |
| 项目速率卡 | `project-limits.rpmLimit/tpmLimit` | 展示真实限额 |
| 用户限制表 | `user-limits` 列表 | `scopeKey→user`；team/预算无则默认 `—` / 0 |
| 登记限制（按模型） | 无对应 API | **保留本地 MOCK 示意行**；live 时不触发「演示路径」 |
| 模型配额条 | 无对应 API | live 时用 project tpm 合成一条「项目 TPM」；demo 用原 MOCK |

### 3.3 A6 后端小修（可写范围内）

`GET /v1/aip/capacity/user-limits`：
- 有 `userId` → 保持单对象（兼容既有测）
- 无 `userId` → `{ items, count }` 列出 `scope=user` 全部限额

不做 schema 大改；不改 `main.py`。

### 3.4 A7 映射

优先读 `GET /v1/aip/model-admin/models`（已含 `registered`）；失败再试 `model-catalog` + `registered-models`。

| API | UI CatalogModel |
|-----|-----------------|
| id | id |
| displayName / model | name |
| provider | provider；slug=lower(provider) |
| contextWindow(int) | 格式化为 `128K` / `2M` |
| inputPrice/outputPrice(float) | `$x/1M`（按 API 存的 per-1K 或 per-token 原值×展示，统一 `formatPricePerMillion`） |
| capabilities | 归一：`text→chat`，`function_calling→function-calling`，未知丢弃 |
| registered | registered |
| parameters | 无则 `—` |

注册按钮：`POST /v1/aip/model-catalog/:id/register` → 成功后刷新；失败 toast/文案，不崩。

已注册 Tab：live 用 `registered===true` 的目录行（或 registered-models 列表）；demo 用原 MODEL_FAMILIES 派生。

### 3.5 样式

`styles.css` 末尾追加：

```css
/* === W2-A6A7 === */
.w2-a6a7-demo-badge { ... }
```

## 4. 可写文件清单

- `apps/web/src/pages/s2/CapacityPage.tsx` (+test)
- `apps/web/src/pages/s2/ModelCatalogPage.tsx` (+test)
- `services/aos-api/aos_api/model_capacity.py`（若需 list 辅助，尽量不改）
- `services/aos-api/aos_api/routers/model_capacity.py`（user-limits 列表）
- `services/aos-api/aos_api/model_catalog.py` / `routers/model_catalog.py`（仅必要时小修）
- `apps/web/src/styles.css` 末尾 `/* === W2-A6A7 === */`
- 交付：`W3_W2_DONE.md`、本方案、`w3(A6A7):` commit

## 5. 测试计划

1. 前端：映射纯函数单测（usage→buckets、catalog row map、capability normalize、sourceMode 判定）。
2. 后端：既有 `test_model_management.py` capacity/catalog 不破；补 user-limits 无 userId 列表断言（若可在归属测文件或引擎测）。
3. 自洽：live 空 usage → 桶为 0 非 mock；fail → 演示路径 + mock。

## 6. 风险与回滚

- 生产若先注册旧 aip_model_capacity_router，phase2 同 path 可能被遮蔽 → 前端失败则自然降级演示路径。
- 按模型速率表无 API，live 仍含本地示意行（非整页演示）。
- 回滚：`git revert` 本分支 `w3(A6A7):` commit。
