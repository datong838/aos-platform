# W3 · W3 C3+C4 属性编辑 + Function 方案

> 版本：v1.0（2026-07-29）  
> 范围：属性编辑 50%→70%；Function 45%→65%  
> 工作树：`aos-platform-w3` / `feature/223-worker-3` / 基线 `b7c8760`

## 0. 适用 Rules

- 用户规则：中文回答；改代码前先完善方案；最小改动；不破坏已有功能；交付后自测
- 227 W3 并行：严格所有权；禁 Wiki*/ObjectType*/pipeline*/source*/nav/main；禁 push/改 m1/merge
- `styles.css` 仅末尾追加 `/* === W3-C3C4 === */`
- commit：`w3(C3C4): ...`

## 1. 目标与验收

| 项 | 标准 |
|----|------|
| C3 列映射 Tab | Tab「列映射」展示映射表；可编辑目标属性；Automap；保存写回 API |
| C3 属性 API | 列表接 `GET .../properties`；新建 `POST .../properties`；失败标演示路径 |
| C4 参数表 | 配置 Tab 参数 CRUD 可用 |
| C4 试跑 | 测试 Tab payload + 运行；优先 API，失败本地 simulate + **「演示路径」** |
| C4 保存 | 接 `PUT /v1/ontology/functions/:id`；失败标演示 |
| 约束 | 不改 `main.py` / `nav.ts` / 他人页面 |

## 2. 现状与缺口

### 2.1 PropertyEditorPage（C3）
- 双栏属性表 + 详情已有；`activeTab` 有 `mapping` 但 **未切换 UI**（两 Tab 同内容）。
- `autoMapAll` 仅本地改 `columnMapping`，未调 `POST .../automap`。
- `saveProperty` 误用 `columnMapping` 区分 create/update；后端无 PUT property。
- 后端已有：`GET/POST properties`、`GET column-mapping`、`POST automap`；引擎有 `set_column_mapping`，**路由缺 PUT**。

### 2.2 FunctionEditorPage（C4）
- 参数表 + 测试面板 UI 已有，数据全 `MOCK_FUNCTIONS`。
- 试跑调 `/test`（单数），后端是 `POST .../tests` + `run`。
- 无保存；无列表加载。后端缺 `GET /functions`；`UpdateFunctionRequest` 无 `params`。

## 3. 设计

### 3.1 数据源模式

```
sourceMode = "live" | "demo"
live  ← 主读接口成功（含空列表）
demo  ← 网络/404 → MOCK/本地 + 「演示路径」角标
```

### 3.2 C3 映射

| UI | API | 规则 |
|----|-----|------|
| 属性表 | `GET .../properties` | `datatype→type`（string→STRING…）；`is_primary_key→isPrimaryKey`；`is_display_name→isTitleKey`；`nullable→!isRequired` |
| 列映射 Tab | `GET .../column-mapping` | 行：source_column / target_property / confidence / status |
| Automap | `POST .../automap` | 成功后刷新映射表，并回写属性 `columnMapping` |
| 保存映射 | `PUT .../column-mapping` | body `{ mappings: [{source_column,target_property,...}] }`（**新增路由**） |
| 新建属性 | `POST .../properties` | AddPropertyRequest 字段；本地 id 以 `new-` 开头判定新建 |
| 已有属性保存 | 无 PUT | live 下仅本地 patch + msg 提示「属性元数据已本地更新；列映射请用映射 Tab 保存」；不伪造成功写库 |

列映射 Tab UI（最小）：
- 表头：源列 / 目标属性 / 置信度 / 状态
- 工具条：Automap、保存映射
- 目标属性可下拉选当前属性名或手输

### 3.3 C4 映射

| UI | API | 规则 |
|----|-----|------|
| 列表 | `GET /v1/ontology/functions` | **新增**；`body→code`；`return_type→outputType`；params `datatype→type`、`default→defaultValue` |
| 保存 | `PUT /functions/:id` | 扩展 body：`display_name/description/body/return_type/status/params` |
| 试跑 | `POST /functions/:id/tests` | `{ name, inputs, run: true }` → 映射为 TestRunResult |
| 失败 | — | `simulateTestRun` + 「演示路径」 |

status 映射：API `active|draft|deprecated` ↔ UI `published|draft|error`（validated 保留本地）。

### 3.4 后端最小改动（可写）

1. `phase4_ontology_types.py`：补 `PUT /object-types/{ot_id}/column-mapping`
2. `phase4_ontology_functions.py`：补 `GET /functions`；`UpdateFunctionRequest` 增 `params`；可选 `POST /functions/{fn_id}/test` 别名（薄封装 tests+run）

不改 `main.py`（router 已挂载）。

### 3.5 样式

`styles.css` 末尾：

```css
/* === W3-C3C4 === */
.w3-c3c4-demo-banner { ... }
.w3-c3c4-demo-badge { ... }
.w3-c3c4-mapping-table ...（必要时）
```

复用间距/字号，不引入新设计体系。

## 4. 可写文件清单

| 文件 | 动作 |
|------|------|
| `apps/web/src/pages/s2/PropertyEditorPage.tsx` | 列映射 Tab + API 映射 + automap/保存 |
| `apps/web/src/pages/s2/PropertyEditorPage.test.ts` | 映射/转换/automap 纯函数测 |
| `apps/web/src/pages/s2/FunctionEditorPage.tsx` | 列表/保存/试跑接线 + 演示角标 |
| `apps/web/src/pages/s2/FunctionEditorPage.test.ts` | API↔UI 映射 + 试跑结果映射 |
| `services/aos-api/aos_api/routers/phase4_ontology_types.py` | PUT column-mapping |
| `services/aos-api/aos_api/routers/phase4_ontology_functions.py` | GET list + params + /test |
| `apps/web/src/styles.css` | 末尾 W3-C3C4 |
| `W3_W3_C3C4_方案.md` / `W3_W3_DONE.md` | 方案与交付 |

## 5. 风险与回滚

| 风险 | 缓解 |
|------|------|
| 属性无 PUT，详情保存不落库 | 文案标明；列映射走独立 PUT |
| Function 引擎未 seed 时列表空 | 空列表仍为 live；仅请求失败才 demo |
| status/mode 字段不完全对齐 | 单向映射 + 默认值，不丢 id |
| 与并行 Worker 冲突 | 只改本 Worker 文件 |

回滚：`git revert` 本分支 `w3(C3C4):` commit。

## 6. 自测计划

1. vitest：`PropertyEditorPage.test.ts` + `FunctionEditorPage.test.ts`
2. 手工逻辑核对：映射表保存 body、automap 刷新、试跑 demo 降级文案
3. 与方案 §1/§3 一致性检查
