# W3 · W4 B5 智能体插件配置+连通测试方案

> 版本：v1.0（2026-07-29）  
> 范围：`/aip/capabilities` 55%→70%  
> 工作树：`aos-platform-w3` / `feature/223-worker-3` / 基线 `4a8ceb8`

## 1. 目标与验收

| 项 | 标准 |
|----|------|
| 列表优先 live | `GET /v1/aip/capabilities` 成功 → 顶栏「真 API」；失败 → 静态卡 + **「演示路径」** |
| 配置可保存 | 配置面板受控表单；`PUT /v1/aip/capabilities/{id}` 写 config；失败本地落盘并标演示 |
| 连通测试可用 | `POST /v1/aip/capabilities/test`；失败本地模拟结果并标演示 |
| 约束 | 不改 `main.py` / `nav.ts` / Link*/Action*/DocumentIntelligence*；`styles.css` 仅末尾 `/* === W4-B5 === */` |

## 2. 现状与缺口

### 2.1 前端 `CapabilityPage.tsx`
- 4 张已接入卡 + 4 种配置表单 UI 齐全，但表单用 `defaultValue`，**保存 / 测连通按钮无 onClick**。
- 列表已调 `GET /v1/aip/capabilities`；Job/Session 演示按钮仍走 `wave_ext` POST（保留，不扩大范围）。

### 2.2 后端
- `phase3_aip_capabilities`：`GET` / `PUT /{id}` 已有；**无** `POST .../test`。
- `GET` 与 `wave_ext` 同路径：FastAPI 先注册 `wave_ext`，列表可能返回 `{id,kind,endpoint}`；phase3 返回 `{id,name,category,config,...}`。前端需兼容两种 shape。
- PUT 对未知 id 现抛 404；插件卡固定 id（`video-job` 等）需 **upsert**，避免首次保存失败。

## 3. 设计

### 3.1 数据源

```
listSource = "live" | "demo"
live  ← GET 成功（含空 items）
demo  ← 网络/鉴权失败 → 仅静态 CAPABILITY_CARDS + 「演示路径」
```

不以空列表判 demo（对齐 A3/A6）。

### 3.2 配置映射（按 cfgType → config dict）

| cfgType | config 关键字段 | 默认 capability id |
|---------|----------------|-------------------|
| job | endpoint, concurrency, secretRef, webhook | `video-job` |
| script | mode, timeoutSec, outputObjectType | `live-script` |
| session | gateway, sessionObject, avExternal, draftGate | `avatar-commerce` |
| http | kind, baseUrl, manifest | `http-adapter` |

点击已接入卡「配置」→ 绑定该卡 `id` + 推断 cfgType，并尝试用 live item 的 `config`/`endpoint` 预填。

### 3.3 后端最小改动

**engine**
- `ensure_plugin_defaults()`：幂等写入 4 插件 + `http-adapter`（稳定 id）。
- `upsert_capability(cap_id, **kwargs)`：不存在则创建（保留传入 id）。
- `test_connectivity(cap_id?, endpoint?)`：校验/回填默认；返回 `{ok,status,latencyMs,endpoint,capabilityId,message}`（进程内模拟，不真实外呼）。

**router**
- `PUT`：改为 upsert（先 ensure defaults）。
- `POST /capabilities/test`：body `{ id|capabilityId?, endpoint? }`。
- 不改 `main.py`（router 已挂载）。

### 3.4 前端行为

| 操作 | live | 失败 |
|------|------|------|
| 刷新列表 | GET → 合并登记态 | demo 角标 + 静态卡 |
| 保存并启用 | PUT config + enabled:true | localStorage `aos.cap.cfg.{id}` + 演示文案 |
| 测连通 | POST test | 本地 `simulateConnectivity` + 演示文案 |
| 卡上「测连通」 | 同上（用卡 id） | 同上 |

导出纯函数供 vitest：`pathLabel` / `defaultConfigFor` / `mergeLiveItem` / `simulateConnectivity` / `formatSaveMsg` / `formatTestMsg`。

### 3.5 样式

`styles.css` 末尾追加 `/* === W4-B5 === */`：source banner/badge、配置面板操作态（busy/disabled 弱样式）。

## 4. 可写文件

- `apps/web/src/pages/CapabilityPage.tsx`
- `apps/web/src/pages/CapabilityPage.test.ts`（新建）
- `services/aos-api/aos_api/aip_capabilities_engine.py`
- `services/aos-api/aos_api/routers/phase3_aip_capabilities.py`
- `services/aos-api/tests/test_phase3_aip_capabilities.py`（同前缀，补 upsert/test）
- `apps/web/src/styles.css`（仅末尾附录）
- `W3_W4_B5_方案.md` / `W3_W4_DONE.md`

## 5. 风险与回滚

- GET 被 `wave_ext` 抢先：列表仍算 live；PUT/test 走 phase3 engine（ensure+upsert 保证 id 存在）。
- 连通测试为进程内健康模拟，非真实外网探活（与现有 phase3 内存引擎一致）。
- 回滚：`git revert` 本分支 `w3(B5):` commit。

## 6. 自测

- vitest：`CapabilityPage.test.ts`
- pytest：`tests/test_phase3_aip_capabilities.py`（upsert + test）
