import { describe, expect, it, vi } from "vitest";
import { EcommerceInvestigationClient } from "./client";

const tenant = { orgId: "org-org", projectId: "dev-project" };
const hash = `sha256:${"a".repeat(64)}`; const ref = (resourceType: string, resourceId: string) => ({ resourceType, resourceId, revision: 1, contentHash: hash });
describe("EcommerceInvestigationClient", () => {
  it("只发出 canonical Case/Run GET 且不提交 tenant", async () => {
    const fetch = vi.fn().mockImplementation(async () => new Response(JSON.stringify({ tenant, items: [], count: 0 }), { status: 200, headers: { "Content-Type": "application/json" } }));
    const client = new EcommerceInvestigationClient({ fetch, getBaseUrl: () => "http://aos.test", getAuthHeaders: () => ({ Authorization: "Bearer test" }) });
    await client.listCases(); await client.listRuns("case:1");
    expect(fetch).toHaveBeenNthCalledWith(1, "http://aos.test/v1/ecommerce/investigations/cases?limit=200", expect.objectContaining({ method: "GET" }));
    expect(fetch).toHaveBeenNthCalledWith(2, "http://aos.test/v1/ecommerce/investigations/cases/case%3A1/runs?limit=200", expect.objectContaining({ method: "GET" }));
    expect(JSON.stringify(fetch.mock.calls)).not.toContain("org-org");
  });
  it("在网络前拒绝非法 Case ID", async () => {
    const fetch = vi.fn(); const client = new EcommerceInvestigationClient({ fetch });
    await expect(client.listRuns("../other")).rejects.toThrow(/caseId 无效/); expect(fetch).not.toHaveBeenCalled();
  });
  it("只用 GET 读取单个 Run 的服务端 Workbench projection", async () => {
    const payload = { schemaVersion: "aos.ecommerce.business-investigation-workbench-view/v4", tenant, projectionHash: hash, sourceWatermark: { caseRevision: 1, runVersion: 1, stateVersion: 1, bindingHashes: [], runtimeHash: null, contentHash: hash }, observedAt: "2026-08-26T08:10:00Z", caseRef: ref("BusinessInvestigationCaseRevision", "case-1"), runRef: ref("BusinessInvestigationRun", "run:1"), stateRef: ref("BusinessInvestigationRunStateRevision", "run:1"), caseEnvelope: { title: "首次分析", analysisType: "initial_store_analysis", lifecycle: "ACTIVE", channelRef: ref("ChannelRevision", "private-mall"), businessEntityRef: ref("BusinessEntityRevision", "store-1"), investigationProfileRef: ref("InvestigationProfileRevision", "profile-1"), scopeRef: ref("InvestigationScopeRevision", "scope-1"), schedulePolicyRef: null, createdBy: "user-1", createdAt: "2026-08-26T08:00:00Z" }, lifecycle: "PREPARING", control: "RUNNING", pendingRequirementRef: null, uncertainCommand: null, runtime: { bindingStatus: "unbound", taskId: null, planRef: null, taskRunRef: null, taskRunStatus: null, checkpoint: null, stages: [{ stageId: "portrait", title: "经营画像", status: "not_started", stepRunId: null, attempt: null }, { stageId: "diagnosis", title: "问题与机会", status: "not_started", stepRunId: null, attempt: null }, { stageId: "solution-design", title: "方案设计", status: "not_started", stepRunId: null, attempt: null }], completed: 0, total: 3, currentStageId: null }, currentWorkspace: { stageId: null, title: "尚无当前阶段", question: "尚未建立 canonical Plan 绑定，当前经营问题未知。", status: "unbound", responsibilitySlotIds: [], assigneeRefs: [], inputRefs: [], outputRefs: [], areas: [{ area: "known", title: "已知与可回链输入", status: "unknown", summary: "缺少可回链输入，已知事实保持未知。", resourceRefs: [], exactRefs: [] }, { area: "unknown", title: "未知与缺口", status: "present", summary: "尚未绑定 canonical Plan。", resourceRefs: [], exactRefs: [] }, { area: "assumption", title: "关键假设", status: "unknown", summary: "尚无专门的假设 authority。", resourceRefs: [], exactRefs: [] }, { area: "counter_evidence", title: "反证与替代解释", status: "unknown", summary: "尚无专门的反证 authority。", resourceRefs: [], exactRefs: [] }], nonClaims: ["可回链输入不等于已确认经营事实。", "当前阶段状态不代表方案已执行。", "不展示模型私有过程。"] }, artifacts: ["BusinessDossierRevision", "ProblemMapRevision", "OpportunityMapRevision", "SolutionPortfolioRevision"].map((artifactType) => ({ artifactType, status: "missing", artifactRef: null, bindingId: null, bindingHash: null, selectionRevision: null, dataCutoff: null, lineageRef: null })), evidence: { status: "missing", exactRefs: [], locatorRefs: [] }, timeline: [{ eventId: "case:case-1:r1", eventType: "case_revision", title: "Case revision 建立", occurredAt: "2026-08-26T08:00:00Z", exactRef: ref("BusinessInvestigationCaseRevision", "case-1"), relatedRef: null }, { eventId: "run:run:1:v1", eventType: "run_created", title: "Run 建立", occurredAt: "2026-08-26T08:01:00Z", exactRef: ref("BusinessInvestigationRun", "run:1"), relatedRef: null }, { eventId: "state:run:1:v1", eventType: "state_revision", title: "Run state revision", occurredAt: "2026-08-26T08:02:00Z", exactRef: ref("BusinessInvestigationRunStateRevision", "run:1"), relatedRef: null }] };
    const fetch = vi.fn().mockResolvedValue(new Response(JSON.stringify(payload), { status: 200, headers: { "Content-Type": "application/json" } })); const client = new EcommerceInvestigationClient({ fetch, getBaseUrl: () => "http://aos.test", getAuthHeaders: () => ({}) });
    const result = await client.getRunView("run:1"); expect(result).toMatchObject({ drilldownVersion: "canonical-v4", runtime: { bindingStatus: "unbound", total: 3 } }); expect(result.timeline[0]?.eventType).toBe("case_revision"); expect(fetch).toHaveBeenCalledWith("http://aos.test/v1/ecommerce/investigations/runs/run%3A1/view", expect.objectContaining({ method: "GET" }));
    const authority = { schemaVersion: "aos.ecommerce.business-investigation-run-state/v1", tenant, runId: "run:1", version: 2, priorRef: ref("BusinessInvestigationRunStateRevision", "run:1"), lifecycle: "PREPARING", control: "PAUSED", eventSequence: 2, contentHash: hash, pendingRequirementRef: null, uncertainCommand: null, createdBy: "user-1", createdAt: "2026-08-26T08:11:00Z" };
    const readback = { ...payload, schemaVersion: "aos.ecommerce.business-investigation-workbench-view/v5", sourceWatermark: { ...payload.sourceWatermark, stateVersion: 2 }, stateRef: { ...payload.stateRef, revision: 2 }, control: "PAUSED", commandProjection: { expectedStateVersion: 2, allowedCommands: ["RESUME_RUN", "CANCEL_RUN"], externalEffectsAllowed: false } };
    fetch.mockResolvedValueOnce(new Response(JSON.stringify({ tenant, authority, replayed: false }), { status: 200, headers: { "Content-Type": "application/json" } })).mockResolvedValueOnce(new Response(JSON.stringify(readback), { status: 200, headers: { "Content-Type": "application/json" } }));
    await expect(client.executeRunCommand({ runId: "run:1", command: "PAUSE_RUN", commandId: "cmd-1", expectedStateVersion: 1 })).resolves.toMatchObject({ commandId: "cmd-1", command: "PAUSE_RUN", authority: { version: 2 }, view: { control: "PAUSED" } });
    expect(fetch).toHaveBeenNthCalledWith(2, "http://aos.test/v1/ecommerce/investigations/runs/run%3A1:pause", expect.objectContaining({ method: "POST", body: "{}", headers: expect.objectContaining({ "Idempotency-Key": "cmd-1", "If-Match": '"1"' }) }));
    expect(fetch).toHaveBeenNthCalledWith(3, "http://aos.test/v1/ecommerce/investigations/runs/run%3A1/view", expect.objectContaining({ method: "GET" }));

    const requirementRef = ref("DataRequirementRevision", "requirement-1");
    const dataAuthority = { ...authority, lifecycle: "WAITING_DATA", control: "RUNNING", pendingRequirementRef: requirementRef };
    const dataResponse = { tenant, requirementRef, requirementStatus: "requested", runAuthority: dataAuthority, dataReplayed: false, runReplayed: false, sourceReadPerformed: false, externalEffectAuthorized: false };
    const dataReadback = { ...payload, schemaVersion: "aos.ecommerce.business-investigation-workbench-view/v5", sourceWatermark: { ...payload.sourceWatermark, stateVersion: 2 }, stateRef: { ...payload.stateRef, revision: 2 }, lifecycle: "WAITING_DATA", pendingRequirementRef: requirementRef, commandProjection: { expectedStateVersion: 2, allowedCommands: ["PAUSE_RUN", "CANCEL_RUN", "CONFIRM_DATA_REQUIREMENT"], externalEffectsAllowed: false } };
    fetch.mockResolvedValueOnce(new Response(JSON.stringify(dataResponse), { status: 200, headers: { "Content-Type": "application/json" } })).mockResolvedValueOnce(new Response(JSON.stringify(dataReadback), { status: 200, headers: { "Content-Type": "application/json" } }));
    const missingData = { purposeCode: "business_portrait_gap", requiredFacts: ["gmv"], timeWindow: { startAt: "2026-08-01T00:00:00Z", endAt: "2026-08-26T00:00:00Z" }, grain: "day", cutoffAt: "2026-08-26T00:00:00Z", freshnessMaxAgeSeconds: 3600, qualityThreshold: 0.95, markings: ["INTERNAL"], minPopulation: 20, acceptableDegradation: ["narrow_time_window"], requestedOutputs: ["DataProductRevision", "EvidenceBundleRevision"], budgetMinor: 1000, expiresAt: "2026-08-27T00:00:00Z" };
    await expect(client.requestMissingData({ runId: "run:1", commandId: "data-cmd-1", expectedStateVersion: 1, body: missingData })).resolves.toMatchObject({ commandId: "data-cmd-1", command: "REQUEST_DATA", response: { requirementRef, sourceReadPerformed: false }, view: { pendingRequirementRef: requirementRef } });
    expect(fetch).toHaveBeenNthCalledWith(4, "http://aos.test/v1/ecommerce/investigations/runs/run%3A1:request-missing-data", expect.objectContaining({ method: "POST", body: JSON.stringify(missingData), headers: expect.objectContaining({ "Idempotency-Key": "data-cmd-1", "If-Match": '"1"' }) }));
    expect(JSON.parse(String(fetch.mock.calls[3]?.[1]?.body))).not.toHaveProperty("tenant");
    expect(JSON.parse(String(fetch.mock.calls[3]?.[1]?.body))).not.toHaveProperty("channelRef");
    expect(fetch).toHaveBeenNthCalledWith(5, "http://aos.test/v1/ecommerce/investigations/runs/run%3A1/view", expect.objectContaining({ method: "GET" }));
  });
  it("命令网络不确定时失败关闭且不自动重放 POST", async () => {
    const fetch = vi.fn().mockRejectedValue(new TypeError("offline")); const client = new EcommerceInvestigationClient({ fetch, getBaseUrl: () => "http://aos.test", getAuthHeaders: () => ({}) });
    await expect(client.executeRunCommand({ runId: "run:1", command: "CANCEL_RUN", commandId: "cmd-unknown", expectedStateVersion: 3 })).rejects.toMatchObject({ code: "COMMAND_OUTCOME_UNKNOWN" });
    expect(fetch).toHaveBeenCalledOnce();
  });
  it("DataRequirement 命令网络不确定时只尝试一次 POST", async () => {
    const fetch = vi.fn().mockRejectedValue(new TypeError("offline")); const client = new EcommerceInvestigationClient({ fetch, getBaseUrl: () => "http://aos.test", getAuthHeaders: () => ({}) });
    await expect(client.confirmDataRequirement({ runId: "run:1", commandId: "data-unknown", expectedStateVersion: 2, decision: "accept" })).rejects.toMatchObject({ code: "COMMAND_OUTCOME_UNKNOWN" });
    expect(fetch).toHaveBeenCalledOnce();
  });
  it("阶段评审按 canonical exact refs 单次提交并强制 GET 回读收敛", async () => {
    const rawHash = "a".repeat(64); const aipRef = (resourceType: string, resourceId: string, revision = 1) => ({ resourceType, resourceId, revision, contentHash: rawHash });
    const openIssue = { tenant, issueId: "review-issue-1", ruleRef: aipRef("EvalRuleRevision", "rule-1"), severity: "error", artifactRef: { artifactId: "artifact-1", contentHash: rawHash }, evalReportRef: aipRef("EvalReportRevision", "eval-1"), location: { stage: "diagnosis", stepRunId: "step-2", attempt: 1 }, evidenceRefs: [aipRef("Evidence", "evidence-1")], suggestedFix: "补齐反证后复核", returnStage: "diagnosis", status: "open", version: 1, createdBy: "reviewer-1", createdAt: "2026-08-26T08:10:00Z", updatedBy: "reviewer-1", updatedAt: "2026-08-26T08:10:00Z" };
    const resolvedIssue = { ...openIssue, status: "resolved", version: 2, updatedAt: "2026-08-26T08:12:00Z" };
    const projection = { tenant, runRef: ref("BusinessInvestigationRun", "run:1"), taskRunRef: { resourceType: "TaskRun", resourceId: "task-run-1", version: 2 }, items: [{ issue: resolvedIssue, stage: "diagnosis", evalReportRef: aipRef("EvalReportRevision", "eval-1"), artifactRef: aipRef("Artifact", "artifact-1"), allowedDecisions: [] }], externalEffectsAllowed: false };
    const response = { tenant, decision: "accept", issue: resolvedIssue, returnDecision: null, replayedOutcomePossible: true, externalEffectsAllowed: false };
    const fetch = vi.fn().mockResolvedValueOnce(new Response(JSON.stringify(response), { status: 200, headers: { "Content-Type": "application/json" } })).mockResolvedValueOnce(new Response(JSON.stringify(projection), { status: 200, headers: { "Content-Type": "application/json" } }));
    const client = new EcommerceInvestigationClient({ fetch, getBaseUrl: () => "http://aos.test", getAuthHeaders: () => ({ Authorization: "Bearer test" }) });
    await expect(client.reviewStage({ runId: "run:1", commandId: "review-command-1", expectedStateVersion: 3, decision: "accept", issueId: "review-issue-1", expectedIssueVersion: 1, reason: "人工复核通过" })).resolves.toMatchObject({ commandId: "review-command-1", response: { issue: { version: 2, status: "resolved" } }, projection: { taskRunRef: { resourceId: "task-run-1" } } });
    expect(fetch).toHaveBeenNthCalledWith(1, "http://aos.test/v1/ecommerce/investigations/runs/run%3A1:review-stage", expect.objectContaining({ method: "POST", headers: expect.objectContaining({ "Idempotency-Key": "review-command-1", "If-Match": '"3"' }), body: JSON.stringify({ decision: "accept", issueId: "review-issue-1", expectedIssueVersion: 1, reason: "人工复核通过" }) }));
    expect(fetch).toHaveBeenNthCalledWith(2, "http://aos.test/v1/ecommerce/investigations/runs/run%3A1/stage-review", expect.objectContaining({ method: "GET" }));
    expect(JSON.stringify(fetch.mock.calls)).not.toContain("org-org");
  });
  it("阶段评审网络不确定时只尝试一次 POST", async () => {
    const fetch = vi.fn().mockRejectedValue(new TypeError("offline")); const client = new EcommerceInvestigationClient({ fetch, getBaseUrl: () => "http://aos.test", getAuthHeaders: () => ({}) });
    await expect(client.reviewStage({ runId: "run:1", commandId: "review-unknown", expectedStateVersion: 3, decision: "return", issueId: "review-issue-1", expectedIssueVersion: 1, reason: "退回补齐证据" })).rejects.toMatchObject({ code: "COMMAND_OUTCOME_UNKNOWN" });
    expect(fetch).toHaveBeenCalledOnce();
  });
  it("确认命令只允许 canonical accept body，驳回才携带 reason", async () => {
    const fetch = vi.fn(); const client = new EcommerceInvestigationClient({ fetch });
    await expect(client.confirmDataRequirement({ runId: "run:1", commandId: "accept-shape", expectedStateVersion: 2, decision: "accept", reason: "不会被持久化" })).rejects.toThrow("accept 不接受 reason");
    await expect(client.confirmDataRequirement({ runId: "run:1", commandId: "reject-shape", expectedStateVersion: 2, decision: "reject" })).rejects.toThrow("reason 无效");
    expect(fetch).not.toHaveBeenCalled();
  });
  it("Handoff 编译只发送用户确认字段并消费服务端派生 exact refs", async () => {
    const rawHash = "a".repeat(64); const artifactTypes = ["BusinessDossierRevision", "ProblemMapRevision", "OpportunityMapRevision", "SolutionPortfolioRevision"];
    const payload = { schemaVersion: "aos.ecommerce.business-investigation-handoff-compile/v1", tenant, runRef: ref("BusinessInvestigationRun", "run:1"), approvedPlanRef: { resourceType: "GrowthPlanRevision", resourceId: "growth-plan-1", revision: 2, contentHash: rawHash }, compilationReceiptRef: ref("BusinessInvestigationCompilationReceipt", "compile-1"), artifactRefs: artifactTypes.map((resourceType, index) => ref(resourceType, `artifact-${index + 1}`)), handoff: { schemaVersion: "aos.ecommerce-workshop.module-handoff-compile/v1", tenant, runId: "task-run-1", taskId: "task-1", evaluatedAt: "2026-08-27T08:00:00Z", responsibilityPlanRef: { resourceType: "ResponsibilityPlanRevision", resourceId: "responsibility-1", revision: 1, contentHash: rawHash }, sourceModuleId: "ecommerce.analyst", targetModuleId: "ecommerce.task-cockpit", sourceSlotId: "slot-analyst", targetSlotId: "slot-cockpit", readiness: "blocked", blockers: [{ code: "TARGET_INSTANCE_NOT_READY", dependency: "slot-cockpit", requiredAction: "重新核验目标职责绑定" }], issueCommand: null, sideEffects: { handoffsIssued: 0, tokensMinted: 0, decisionsCreated: 0, agentRunsStarted: 0 } } };
    const fetch = vi.fn().mockResolvedValue(new Response(JSON.stringify(payload), { status: 200, headers: { "Content-Type": "application/json" } })); const client = new EcommerceInvestigationClient({ fetch, getBaseUrl: () => "http://aos.test", getAuthHeaders: () => ({ Authorization: "Bearer test" }) });
    const input = { handoffId: "handoff-1", approvedPlanRef: { resourceType: "GrowthPlanRevision" as const, resourceId: "growth-plan-1", revision: 2, contentHash: rawHash }, sourceSlotId: "slot-analyst", targetModuleId: "ecommerce.task-cockpit" as const, targetSlotId: "slot-cockpit", purpose: "受控承接", requestedOutcome: "返回决定", markings: ["INTERNAL"], expiresAt: "2026-08-27T08:15:00Z" };
    const result = await client.compileHandoff("run:1", input); expect(result.artifactRefs).toHaveLength(4); expect(result.artifactRefs[0]?.resourceType).toBe("BusinessDossierRevision"); expect(result.handoff.readiness).toBe("blocked");
    expect(fetch).toHaveBeenCalledWith("http://aos.test/v1/ecommerce/investigations/runs/run%3A1/handoffs:compile", expect.objectContaining({ method: "POST", body: JSON.stringify(input) }));
    expect(String(fetch.mock.calls[0]?.[1]?.body)).not.toContain("org-org"); expect(String(fetch.mock.calls[0]?.[1]?.body)).not.toContain("task-run-1"); expect(String(fetch.mock.calls[0]?.[1]?.body)).not.toContain("artifact-1");
  });
  it("Handoff 编译网络不确定时只尝试一次且不伪造成功", async () => {
    const rawHash = "a".repeat(64); const fetch = vi.fn().mockRejectedValue(new TypeError("offline")); const client = new EcommerceInvestigationClient({ fetch, getBaseUrl: () => "http://aos.test", getAuthHeaders: () => ({}) });
    await expect(client.compileHandoff("run:1", { handoffId: "handoff-unknown", approvedPlanRef: { resourceType: "GrowthPlanRevision", resourceId: "growth-plan-1", revision: 2, contentHash: rawHash }, sourceSlotId: "slot-analyst", targetModuleId: "ecommerce.task-cockpit", targetSlotId: "slot-cockpit", purpose: "受控承接", requestedOutcome: "返回决定", markings: ["INTERNAL"], expiresAt: "2026-08-27T08:15:00Z" })).rejects.toMatchObject({ code: "COMMAND_OUTCOME_UNKNOWN" });
    expect(fetch).toHaveBeenCalledOnce();
  });
});
