import { describe, expect, it } from "vitest";
import { EcommerceWorkshopClient } from "./client";

describe("media studio strict client", () => {
  it("调用 GET 并保留 target 非 ready", async () => { const payload = { schemaVersion: "aos.ecommerce-workshop.media-studio-view/v1", tenant: { orgId: "org-org", projectId: "dev-project" }, evaluatedAt: "2026-08-24T08:00:00Z", dataCutoff: "2026-08-24T08:00:00Z", readiness: "degraded", slices: ["context", "execution", "delivery"].map((sliceId) => ({ sliceId, status: "blocked", dataCutoff: "2026-08-24T08:00:00Z", readinessAxes: ["module", "capability", "assignee", "provider", "budget", "publication"].map((axis) => ({ axis, status: "target", exactRef: null, targetContractRef: `ADR-86#${axis}`, gaps: ["missing"], blockers: [{ code: "MEDIA_AUTHORITY_NOT_AVAILABLE", dependency: axis, requiredAction: "attach exact ref" }] })), authorityRefs: [], blockers: [{ code: `MEDIA_${sliceId.toUpperCase()}_AUTHORITY_NOT_AVAILABLE`, dependency: sliceId, requiredAction: "attach exact refs" }], countLedger: { denominator: 6, ready: 0, target: 6, blocked: 0, unknown: 0, conflict: 0, notApplicable: 0 } })), page: { limit: 100, count: 0, hasMore: false, nextCursor: null } }; const calls: RequestInit[] = []; const client = new EcommerceWorkshopClient({ fetch: async (_input, init) => { calls.push(init ?? {}); return new Response(JSON.stringify(payload), { status: 200, headers: { "Content-Type": "application/json" } }); }, getBaseUrl: () => "http://example.test", getAuthHeaders: () => ({}) }); expect((await client.getMediaStudioView()).slices).toHaveLength(3); expect(calls[0]?.method).toBe("GET"); });
});
