# M5 Ecommerce Bundle Fixtures

These directories contain unsigned, test-only bundle skeletons used to verify
the generic AOS asset lifecycle. They do not contain executable integrations,
platform mappings, business logic, credentials, or production configuration.

The files under `content/` are placeholders only. Signatures are generated in
temporary test directories at runtime and are never committed here.
